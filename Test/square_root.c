#include<stdio.h>
#include<conio.h>
#include<math.h>
int main(){
    int number;
    int sr;
    printf("Enter a number: ");
    scanf("%d",&number);
    sr=sqrt(number);
    printf("Square root of the given number is %d",sr);
}